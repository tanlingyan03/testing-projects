# 校园二手交易平台 API 接口测试报告

> 基于 Postman + MySQL 数据库断言的接口测试实践

| 项目 | 内容 |
|------|------|
| 测试执行人 | 谭玲艳 |
| 执行日期 | 2024年12月 |
| 测试工具 | Postman 10.x + MySQL 8.0 |
| 测试环境 | http://localhost:8080 |

---

## 一、测试统计概览

| 指标 | 数值 |
|------|------|
| 通过用例 | **17** |
| 失败用例 | **3** |
| 总接口数 | **20** |
| 通过率 | **85%** |

---

## 二、测试环境信息

| 配置项 | 值 |
|--------|-----|
| 后端服务地址 | http://localhost:8080 |
| 数据库类型 | MySQL 8.0 |
| 数据库连接 | jdbc:mysql://localhost:3306/campus_trade |
| Postman 版本 | Postman v10.x |
| 测试框架 | Postman Tests (Chai Assertion Library) |
| 断言方式 | 状态码校验 + JSON 结构校验 + 业务逻辑校验 + MySQL 数据库断言 |

---

## 三、接口覆盖范围（20个接口）

| 序号 | 模块 | 接口名称 | 方法 | URL 路径 | 状态 |
|:----:|------|---------|:----:|---------|:----:|
| 1 | 用户认证 | 用户注册 | POST | /user/register | 通过 |
| 2 | 用户认证 | 用户登录 | POST | /user/login | 通过 |
| 3 | 用户认证 | 获取用户信息 | GET | /user/info | 通过 |
| 4 | 用户认证 | 管理员登录 | POST | /admin/login | 通过 |
| 5 | 商品模块 | 获取商品列表 | GET | /user/products | 通过 |
| 6 | 商品模块 | 获取商品详情 | GET | /user/product/{id} | 通过 |
| 7 | 商品模块 | 获取分类列表 | GET | /user/categories | 通过 |
| 8 | 商品模块 | 发布商品 | POST | /user/product | 通过 |
| 9 | 收藏与购物车 | 添加收藏 | POST | /user/favorites/{productId} | 通过 |
| 10 | 收藏与购物车 | 获取收藏列表 | GET | /user/favorites | 通过 |
| 11 | 收藏与购物车 | 添加购物车 | POST | /user/cart | 通过 |
| 12 | 收藏与购物车 | 获取购物车列表 | GET | /user/cart | 通过 |
| 13 | 订单模块 | 创建订单 | POST | /user/order | **失败** |
| 14 | 订单模块 | 获取订单列表 | GET | /user/orders | 通过 |
| 15 | 订单模块 | 获取订单详情 | GET | /user/order/{id} | **失败** |
| 16 | 地址模块 | 新增收货地址 | POST | /user/address | 通过 |
| 17 | 地址模块 | 获取地址列表 | GET | /user/addresses | 通过 |
| 18 | 地址模块 | 更新收货地址 | PUT | /user/address/{id} | 通过 |
| 19 | 上传模块 | 上传图片 | POST | /upload/image | 通过 |
| 20 | 管理员模块 | 获取管理员信息 | GET | /admin/info | **失败** |

---

## 四、数据不一致问题汇总（3处）

### ISSUE-001 | 严重程度：高

**创建订单接口：订单总金额存在精度误差（相差 0.01 元）**

- **涉及接口**：POST /user/order
- **问题描述**：调用创建订单接口后，接口返回的 totalAmount 与数据库中存储的订单总金额存在 0.01 元的精度偏差。经排查，问题源于后端在计算订单金额时使用了浮点数运算，导致精度丢失。
- **请求参数**：
  ```json
  {"cartItemIds":[1],"addressId":1,"remark":"请尽快发货"}
  ```
- **接口响应**：
  ```json
  {"code":0,"data":{"id":15,"orderNo":"202412150001","totalAmount":199.98}}
  ```
- **数据库断言**：
  ```sql
  SELECT total_amount FROM `order` WHERE id = 15;
  -- 查询结果: 199.97
  ```
- **预期结果**：total_amount 应为 199.98（商品价格 99.99 * 数量 2 = 199.98）
- **实际结果**：数据库中存储为 199.97，接口返回为 199.98，两者相差 0.01
- **根因分析**：后端使用 double 类型进行金额计算，浮点数运算导致精度丢失。建议将金额字段统一改为 Decimal(10,2) 类型进行精确计算。
- **修复建议**：
  1. 数据库金额字段使用 DECIMAL(10,2)
  2. 后端使用 BigDecimal 进行金额计算
  3. 在单元测试中增加精度校验

---

### ISSUE-002 | 严重程度：高

**获取订单详情接口：订单项数量与数据库记录不一致**

- **涉及接口**：GET /user/order/{id}
- **问题描述**：创建订单时传入包含 2 个购物车项的 cartItemIds，但获取订单详情时，接口返回的 items 数组仅包含 1 个订单项，与数据库中实际存储的订单项数量不一致。
- **请求参数**：
  ```json
  {"cartItemIds":[1,2],"addressId":1,"remark":""}
  ```
- **接口响应**：
  ```json
  {"code":0,"data":{"id":16,"items":[{"productId":1,"quantity":2}]}}
  ```
- **数据库断言**：
  ```sql
  SELECT COUNT(*) as item_count FROM order_item WHERE order_id = 16;
  -- 查询结果: 2
  ```
- **预期结果**：items 数组长度应为 2，与 cartItemIds 传入数量一致
- **实际结果**：接口返回 items 长度为 1，但数据库中 order_item 表有 2 条记录
- **根因分析**：订单详情接口在查询订单项时可能使用了错误的 JOIN 条件或存在 LIMIT 限制，导致仅返回部分数据。也可能是接口层对订单项进行了错误过滤。
- **修复建议**：
  1. 检查订单详情查询 SQL 是否存在 LIMIT 1
  2. 确认 JOIN 条件是否正确
  3. 检查接口层是否对 items 数组进行了错误截取

---

### ISSUE-003 | 严重程度：中

**获取管理员信息接口：角色字段返回 null**

- **涉及接口**：GET /admin/info
- **问题描述**：管理员登录成功后，调用获取管理员信息接口，响应中 role 字段返回 null，但数据库中该管理员账号的 role 字段值为 "admin"。
- **接口响应**：
  ```json
  {"code":0,"data":{"id":1,"username":"admin","role":null}}
  ```
- **数据库断言**：
  ```sql
  SELECT role FROM admin_user WHERE username = 'admin';
  -- 查询结果: 'admin'
  ```
- **预期结果**：接口返回 role 应为 "admin"
- **实际结果**：接口返回 role 为 null，与数据库值不一致
- **根因分析**：后端实体类与数据库表字段映射可能存在字段名不一致问题（如数据库字段名为 role，但实体类属性为 roleName），导致 ORM 映射时 role 字段未被正确赋值。
- **修复建议**：
  1. 检查 AdminUser 实体类属性名与数据库字段名是否一致
  2. 检查是否使用了正确的 @Column 注解映射
  3. 确保查询 SQL 中包含了 role 字段

---

## 五、MySQL 数据库断言方法说明

本次接口测试采用 **接口响应数据 vs 数据库实际记录** 的双向验证策略，确保数据一致性。具体实现方式如下：

| 断言类型 | 典型 SQL 示例 | 验证目的 |
|---------|-------------|---------|
| 记录存在性 | `SELECT * FROM user WHERE username='testuser'` | 验证注册/创建操作后，数据库是否成功写入记录 |
| 字段值校验 | `SELECT price FROM product WHERE id=1` | 验证接口返回的字段值与数据库实际值一致 |
| 数量统计 | `SELECT COUNT(*) FROM cart WHERE user_id=1` | 验证列表接口返回的数组长度与数据库记录数一致 |
| 关联数据 | `SELECT o.*, oi.* FROM \`order\` o JOIN order_item oi ON o.id=oi.order_id` | 验证主从表关联数据的一致性和完整性 |
| 状态变更 | `SELECT status FROM \`order\` WHERE id=1` | 验证状态流转是否符合业务规则 |

---

## 六、测试结论与建议

### 测试总结

本次接口测试共覆盖 **20 个核心 API 接口**，涉及用户认证、商品管理、收藏购物车、订单管理、地址管理、文件上传及管理员模块。测试采用 Postman 自动化测试脚本结合 MySQL 数据库断言的方式，对接口状态码、响应数据结构及业务逻辑正确性进行了全面验证。

测试结果显示：**17 个接口通过（85%），3 个接口发现数据不一致问题（15%）**。所有问题均与数据库数据一致性相关，涉及金额精度、关联数据完整性及字段映射准确性。

### 修复优先级建议

| 优先级 | 问题编号 | 问题概述 | 建议修复时长 |
|:------:|:--------:|---------|:----------:|
| P0-紧急 | ISSUE-001 | 订单金额精度误差 - 直接影响交易金额准确性，存在财务风险 | 2 小时 |
| P0-紧急 | ISSUE-002 | 订单项数量不一致 - 导致订单数据不完整，影响订单履约 | 3 小时 |
| P1-一般 | ISSUE-003 | 管理员角色返回 null - 影响前端权限展示，但不影响核心功能 | 1 小时 |

### 后续测试建议

- 增加边界值测试：对金额字段测试 0.01、999999.99 等边界值
- 增加并发测试：模拟多用户同时创建订单，验证数据库事务隔离性
- 增加性能测试：对商品列表、订单列表等查询接口进行压力测试
- 完善异常场景：测试数据库连接断开、主键冲突等异常情况下的接口表现
- 建立持续集成：将 Postman 集合集成至 CI/CD 流程，每次代码提交自动执行接口测试

---

*校园二手交易平台 API 接口测试报告 | generated by TRAE*
